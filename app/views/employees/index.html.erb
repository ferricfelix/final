<div>
	<p> Employees </p>
</div>
<p>
<%= link_to "Create New Employee", new_employee_path %>
</p>
<div>
<% @employees.each do |employee| %>
	
	<div>
		<%= link_to "#{employee.person.first_name + " " + employee.person.last_name + " | " + employee.department.name}", employee_path(employee) %>
	</div>

<% end %>
</div>